import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Dekore = () => {
    const data = useLoaderData();
    return (
        <div>
            <h1>aaaaaaaaaaaaa</h1>
        </div>
    );
};

export default Dekore;